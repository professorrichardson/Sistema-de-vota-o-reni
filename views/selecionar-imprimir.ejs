<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Selecionar Projeto para Imprimir</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        .project-card {
            transition: transform 0.2s;
            cursor: pointer;
        }
        .project-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>🖨️ Selecionar Projeto para Imprimir</h1>
            <a href="/" class="btn btn-secondary">← Voltar</a>
        </div>

        <% if (projetos.length === 0) { %>
            <div class="text-center py-5">
                <p class="text-muted">Nenhum projeto cadastrado ainda.</p>
                <a href="/" class="btn btn-primary">Cadastrar Primeiro Projeto</a>
            </div>
        <% } else { %>
            <div class="row">
                <% projetos.forEach(projeto => { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card project-card h-100" onclick="window.location.href='/imprimir/<%= projeto.id %>'">
                            <div class="card-body text-center">
                                <h5 class="card-title"><%= projeto.nome %></h5>
                                <p class="card-text">
                                    <span class="badge bg-primary"><%= projeto.total_votos %> votos</span>
                                </p>
                                <div class="mt-3">
                                    <img src="/qrcode/<%= projeto.id %>" alt="QR Code" class="img-fluid" style="max-width: 120px;">
                                </div>
                                <button class="btn btn-primary mt-3">Selecionar para Impressão</button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>