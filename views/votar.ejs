<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votação - <%= schoolName %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        body { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh; display: flex; align-items: center;
        }
        .voting-card { background: white; border-radius: 15px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); }
        .logo-votacao { max-height: 50px; width: auto; margin-bottom: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="voting-card p-5 text-center">
                    <img src="/img/logo.png" alt="Logo do Colégio" class="logo-votacao">
                    <h1 class="mb-3">🗳️ Votação</h1>
                    <h2 class="text-primary mb-4"><%= projeto.nome %></h2>
                    
                    <form action="/votar" method="POST" id="votingForm">
                        <input type="hidden" name="projetoId" value="<%= projeto.id %>">
                        <button type="submit" class="btn btn-success btn-lg px-5 py-3">
                            ✅ Votar neste Projeto
                        </button>
                    </form>
                    
                    <p class="text-muted mt-4">
                        <%= schoolName %>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('votingForm').addEventListener('submit', function(e) {
            const button = this.querySelector('button[type="submit"]');
            button.disabled = true;
            button.innerHTML = '⏳ Registrando voto...';
        });
    </script>
</body>
</html>