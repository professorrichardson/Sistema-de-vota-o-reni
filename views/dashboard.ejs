<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Dashboard de Votação</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Dashboard de Votação</h1>
        <hr>
        
        <h2 class="mt-4">Cadastrar Novo Projeto</h2>
        <form action="/cadastrar" method="POST" class="d-flex gap-2 mb-4">
            <input type="text" name="nome" placeholder="Nome do Projeto" class="form-control" required>
            <button type="submit" class="btn btn-primary">Cadastrar</button>
        </form>

        <h2 class="mt-4">Projetos Cadastrados</h2>
        <div class="row">
            <% projetos.forEach(projeto => { %>
                <div class="col-md-4 mb-4">
                    <div class="card text-center">
                        <div class="card-body">
                            <h5 class="card-title"><%= projeto.nome %></h5>
                            <p class="card-text">Escaneie o QR Code para votar.</p>
                            <img src="/qrcode/<%= projeto.id %>" alt="QR Code" class="img-fluid mb-3">
                            <a href="/resultados/<%= projeto.id %>" class="btn btn-secondary">Ver Resultados</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>